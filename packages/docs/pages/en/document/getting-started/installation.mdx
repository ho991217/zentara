# Installation

```bash npm2yarn
npm install @zentara/core
```

## Installing Plugins

Zentara can be extended with plugins. For example, to install the suggestions plugin:

```bash npm2yarn
npm install @zentara/plugin-suggestions
```
